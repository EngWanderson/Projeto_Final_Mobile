<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.academiafinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Escolha_exercicio"></activity>
        <activity android:name=".TreinoSegunda" />
        <activity android:name=".Treinos" />
        <activity android:name=".EditarMedidas1" />
        <activity android:name=".VerMedidas" />
        <activity android:name=".Nova_medida" />
        <activity android:name=".TelaMedidas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>